[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"

[[plugins]]
  # Essential Next.js plugin — handles routing, ISR, and middleware
  package = "@netlify/plugin-nextjs"
